---
title: "Properties (Toolbar Manager Interface)"
---

# Properties

80 properties from `ToBarManagerInt.chm`.

| Name | Summary |
| --- | --- |
| [`ToActionObjButtonInt.BaseID`](toactionobjbuttonint-baseid.md) | Specifies the name of the base type of a bar element. There are base types for user-defined bar elements and predefined base types for a specific purpose. User-defined base types always start with the ID Custom , such as CustomButton and CustomPopup . |
| [`ToActionObjButtonInt.Caption`](toactionobjbuttonint-caption.md) | Specifies the label of a bar element. Use the Style property to specify the label. |
| [`ToActionObjButtonInt.Enable`](toactionobjbuttonint-enable.md) | Specifies whether the button can be enabled. DIAdem dims buttons that cannot be enabled. Note DIAdem does not save the Enable property in the bar definition. Note Use the Enable property only within the OnRefreshCode event. |
| [`ToActionObjButtonInt.ID`](toactionobjbuttonint-id.md) | Specifies the name of the bar element in the ActionObj collection. DIAdem does not distinguish between uppercase and lowercase for the specified name. The name is unique in the ActionObj collection. |
| [`ToActionObjButtonInt.Kind`](toactionobjbuttonint-kind.md) | Specifies whether the bar element is a predefined or a user-defined bar element. |
| [`ToActionObjButtonInt.LanguageId`](toactionobjbuttonint-languageid.md) | Specifies the scripting language used for the OnClickCode and OnRefreshCode events of the bar element. Possible values are "VBS" (VBScript) and "PY" (Python). The default value is "VBS" . |
| [`ToActionObjButtonInt.OnClickCode`](toactionobjbuttonint-onclickcode.md) | Specifies the VBS code that DIAdem executes when the bar element is clicked. Within the VBS code you can use and change the properties of the object. Use the This variable to access the object. |
| [`ToActionObjButtonInt.OnRefreshCode`](toactionobjbuttonint-onrefreshcode.md) | Contains the VBS code of a button, which DIAdem executes during the refresh process. DIAdem executes a refresh, for example, when you switch a panel, open a bar, or click a button. The refresh also executes if you specify a refresh command of the bar manager with a script. If the button is on one of the two bars that are always visible in a panel, you must execute the refresh. Within the VBS code you can use and change the properties of the object. Use the This variable to access the object. |
| [`ToActionObjButtonInt.Picture`](toactionobjbuttonint-picture.md) | Specifies which graphic appears for the button on the DIAdem interface. For DIAdem to display a graphic, the graphics file must contain a symbol that is same size as the properties ActionObjPictureHeight and ActionObjPictureWidth of the bar that is to contain the symbol. DIAdem uses 16*16 pixels as the default symbol size for the toolbar and 32*32 pixels for the group bar. |
| [`ToActionObjButtonInt.Shape`](toactionobjbuttonint-shape.md) | Specifies how DIAdem displays a bar element in a bar. Note If a bar element is predefined, which means the Child property has the value eActObjKindPreset , this property is write protected. If the bar element is user-defined ( Child =eActObjKindCustom ), you can change this property. |
| [`ToActionObjButtonInt.Tooltip`](toactionobjbuttonint-tooltip.md) | Specifies the tooltip for a bar element. If you idle the mouse on a bar element, the tooltip appears. |
| [`ToActionObjButtonInt.Type`](toactionobjbuttonint-type.md) | Returns the bar element type. |
| [`ToActionObjButtonInt.Visible`](toactionobjbuttonint-visible.md) | Specifies whether the bar element is visible. Note DIAdem does not save the Visible property in the bar definition. Note Use the Visible property only within the OnRefreshCode event. |
| [`ToActionObjCheckInt.BaseID`](toactionobjcheckint-baseid.md) | Specifies the name of the base type of a bar element. There are base types for user-defined bar elements and predefined base types for a specific purpose. User-defined base types always start with the ID Custom , such as CustomButton and CustomPopup . |
| [`ToActionObjCheckInt.Caption`](toactionobjcheckint-caption.md) | Specifies the label of a bar element. Use the Style property to specify the label. |
| [`ToActionObjCheckInt.Check`](toactionobjcheckint-check.md) | Specifies whether the button that displays a status is enabled in the DIAdem interface. |
| [`ToActionObjCheckInt.Enable`](toactionobjcheckint-enable.md) | Specifies whether the button that displays a status can be enabled. DIAdem dims buttons that cannot be enabled. Note DIAdem does not save the Enable property in the bar definition. Note Use the Enable property only within the OnRefreshCode event. |
| [`ToActionObjCheckInt.ID`](toactionobjcheckint-id.md) | Specifies the name of the bar element in the ActionObj collection. DIAdem does not distinguish between uppercase and lowercase for the specified name. The name is unique in the ActionObj collection. |
| [`ToActionObjCheckInt.Kind`](toactionobjcheckint-kind.md) | Specifies whether the bar element is a predefined or a user-defined bar element. |
| [`ToActionObjCheckInt.LanguageId`](toactionobjcheckint-languageid.md) | Specifies the scripting language used for the OnClickCode and OnRefreshCode events of the bar element. Possible values are "VBS" (VBScript) and "PY" (Python). The default value is "VBS" . |
| [`ToActionObjCheckInt.OnClickCode`](toactionobjcheckint-onclickcode.md) | Specifies the VBS code that DIAdem executes when the bar element is clicked. Within the VBS code you can use and change the properties of the object. Use the This variable to access the object. |
| [`ToActionObjCheckInt.OnRefreshCode`](toactionobjcheckint-onrefreshcode.md) | Contains the VBS code of a button, which DIAdem executes during the refresh process. DIAdem executes a refresh, for example, when you switch a panel, open a bar, or click a button. The refresh also executes if you specify a refresh command of the bar manager with a script. If the button is on one of the two bars that are always visible in a panel, you must execute the refresh. Within the VBS code you can use and change the properties of the object. Use the This variable to access the object. |
| [`ToActionObjCheckInt.Picture`](toactionobjcheckint-picture.md) | Specifies which graphic appears for the button that displays a status on the DIAdem interface. For DIAdem to display a graphic, the graphics file must contain a symbol that is same size as the properties ActionObjPictureHeight and ActionObjPictureWidth of the bar that is to contain the symbol. DIAdem uses 16*16 pixels as the default symbol size for the toolbar and 32*32 pixels for the group bar. |
| [`ToActionObjCheckInt.Shape`](toactionobjcheckint-shape.md) | Specifies how DIAdem displays a bar element in a bar. Note If a bar element is predefined, which means the Child property has the value eActObjKindPreset , this property is write protected. If the bar element is user-defined ( Child =eActObjKindCustom ), you can change this property. |
| [`ToActionObjCheckInt.Tooltip`](toactionobjcheckint-tooltip.md) | Specifies the tooltip for a bar element. If you idle the mouse on a bar element, the tooltip appears. |
| [`ToActionObjCheckInt.Type`](toactionobjcheckint-type.md) | Returns the bar element type. |
| [`ToActionObjCheckInt.Visible`](toactionobjcheckint-visible.md) | Specifies whether the button that displays a status is visible. Note DIAdem does not save the Visible property in the bar definition. Note Use the Visible property only within the OnRefreshCode event. |
| [`ToActionObjListInt.Count`](toactionobjlistint-count.md) | Returns the total number of available bar elements in DIAdem. |
| [`ToActionObjPopupInt.BarID`](toactionobjpopupint-barid.md) | Specifies the name of the bar that DIAdem opens when the button is clicked. |
| [`ToActionObjPopupInt.BaseID`](toactionobjpopupint-baseid.md) | Specifies the name of the base type of a bar element. There are base types for user-defined bar elements and predefined base types for a specific purpose. User-defined base types always start with the ID Custom , such as CustomButton and CustomPopup . |
| [`ToActionObjPopupInt.Caption`](toactionobjpopupint-caption.md) | Specifies the label of a bar element. Use the Style property to specify the label. |
| [`ToActionObjPopupInt.Enable`](toactionobjpopupint-enable.md) | Specifies whether the button that opens a bar is enabled. DIAdem dims buttons that cannot be enabled. Note DIAdem does not save the Enable property in the bar definition. Note Use the Enable property only within the OnRefreshCode event. |
| [`ToActionObjPopupInt.ID`](toactionobjpopupint-id.md) | Specifies the name of the bar element in the ActionObj collection. DIAdem does not distinguish between uppercase and lowercase for the specified name. The name is unique in the ActionObj collection. |
| [`ToActionObjPopupInt.Kind`](toactionobjpopupint-kind.md) | Specifies whether the bar element is a predefined or a user-defined bar element. |
| [`ToActionObjPopupInt.LanguageId`](toactionobjpopupint-languageid.md) | Specifies the scripting language used for the OnRefreshCode event of the toolbar element. Possible values are "VBS" (VBScript) and "PY" (Python).  The default value is "VBS". |
| [`ToActionObjPopupInt.OnRefreshCode`](toactionobjpopupint-onrefreshcode.md) | Contains the VBS code of a button, which DIAdem executes during the refresh process. DIAdem executes a refresh, for example, when you switch a panel, open a bar, or click a button. The refresh also executes if you specify a refresh command of the bar manager with a script. Within the VBS code you can use and change the properties of the object. Use the This variable to access the object. |
| [`ToActionObjPopupInt.Picture`](toactionobjpopupint-picture.md) | Specifies which graphic appears for the button that opens a bar on the DIAdem interface. For DIAdem to display a graphic, the graphics file must contain a symbol that is same size as the properties ActionObjPictureHeight and ActionObjPictureWidth of the bar that is to contain the symbol. DIAdem uses 16*16 pixels as the default symbol size for the toolbar and 32*32 pixels for the group bar. |
| [`ToActionObjPopupInt.Shape`](toactionobjpopupint-shape.md) | Specifies how DIAdem displays a bar element in a bar. Note If a bar element is predefined, which means the Child property has the value eActObjKindPreset , this property is write protected. If the bar element is user-defined ( Child =eActObjKindCustom ), you can change this property. |
| [`ToActionObjPopupInt.Tooltip`](toactionobjpopupint-tooltip.md) | Specifies the tooltip for a bar element. If you idle the mouse on a bar element, the tooltip appears. |
| [`ToActionObjPopupInt.Type`](toactionobjpopupint-type.md) | Returns the bar element type. |
| [`ToActionObjPopupInt.Visible`](toactionobjpopupint-visible.md) | Specifies whether the button that opens a bar is visible. Note DIAdem does not save the Visible property in the bar definition. Note Use the Visible property only within the OnRefreshCode event. |
| [`ToActionObjSeparatorInt.BaseID`](toactionobjseparatorint-baseid.md) | Specifies the name of the base type of a bar element. There are base types for user-defined bar elements and predefined base types for a specific purpose. User-defined base types always start with the ID Custom , such as CustomButton and CustomPopup . |
| [`ToActionObjSeparatorInt.ID`](toactionobjseparatorint-id.md) | Specifies the name of the bar element in the ActionObj collection. DIAdem does not distinguish between uppercase and lowercase for the specified name. The name is unique in the ActionObj collection. |
| [`ToActionObjSeparatorInt.Type`](toactionobjseparatorint-type.md) | Returns the bar element type. |
| [`ToBarInt.ActionObjPictureHeight`](tobarint-actionobjpictureheight.md) | Specifies the height of the graphics that DIAdem displays on the elements of a bar, in pixels. This way, the property ensures the uniform appearance of the bar. The specifications ActionObjPictureWidth and ActionObjPictureHeight refer to a screen resolution of 96 DPI. With other screen resolutions, DIAdem either scales the graphic or uses a matching symbol size from the ICO file. For example, if the resolution is 44 DPI, DIAdem scales the graphic by 150% or uses a 24 pixel-sized symbol, if that is available in the ICO file, for 16 pixel-sized specifications. |
| [`ToBarInt.ActionObjPictureWidth`](tobarint-actionobjpicturewidth.md) | Specifies the width of the graphics that DIAdem displays on the elements of a bar, in pixels. This way, the property ensures the uniform appearance of the bar. The specifications ActionObjPictureWidth and ActionObjPictureHeight refer to a screen resolution of 96 DPI. With other screen resolutions, DIAdem either scales the graphic or uses a matching symbol size from the ICO file. For example, if the resolution is 44 DPI, DIAdem scales the graphic by 150% or uses a 24 pixel-sized symbol, if that is available in the ICO file, for 16 pixel-sized specifications. |
| [`ToBarInt.ID`](tobarint-id.md) | Returns the bar name, which is unique in the Bars collection. |
| [`ToBarInt.Position`](tobarint-position.md) | Specifies where the bar is positioned in the higher-ranking window. |
| [`ToBarInt.Style`](tobarint-style.md) | Specifies how the bar elements are displayed within a bar. |
| [`ToBarInt.UsedActionObjs`](tobarint-usedactionobjs.md) | Returns a collection of the elements of a bar. |
| [`ToBarInt.Visible`](tobarint-visible.md) | Specifies whether a bar is visible. |
| [`ToBarListInt.Count`](tobarlistint-count.md) | Returns the total number of bars that DIAdem contains. |
| [`ToBarManagerInt.ActionObjs`](tobarmanagerint-actionobjs.md) | Returns a collection of all bar elements of an additional bar manager, which is not used for the bar display in DIAdem. To access an individual element in scripts, use the Item property, or enter the index or the name in parentheses. |
| [`ToBarManagerInt.Bars`](tobarmanagerint-bars.md) | Returns a collection of all bars of an additional bar manager, which is not used for the bar display in DIAdem. To access an individual bar in a script, use the Item property or enter the index or the name in parentheses. |
| [`ToBarManagerInt.DataPortal`](tobarmanagerint-dataportal.md) | Returns the Data Portal. |
| [`ToBarManagerInt.FileName`](tobarmanagerint-filename.md) | Returns the name of the current bar definition of an additional bar manager, which is not used for the bar display in DIAdem. The filename extension is irrelevant. Note If you do not have your own bar definition, this property contains the name Desktop . |
| [`ToBarManagerInt.FilePath`](tobarmanagerint-filepath.md) | Returns the absolute path to the current bar definition of an additional bar manager, which is not used for the bar display in DIAdem. Note If you do not have your own bar definition, this property contains the name of the desktop folder. |
| [`ToBarManagerInt.Panels`](tobarmanagerint-panels.md) | Returns a collection of all panels that can contain bars for an additional bar manager. To access an individual panel in scripts, use the Item property, or enter the index or the name in parentheses. |
| [`ToBarManagerInt.ResourcePath`](tobarmanagerint-resourcepath.md) | Returns the path to the folder that contains the user-defined ICO files. |
| [`ToBarManagerInt.Shell`](tobarmanagerint-shell.md) | Returns the DIAdem main window. |
| [`ToBarManagerUIInt.ActionObjs`](tobarmanageruiint-actionobjs.md) | Returns a collection of all the bar elements in DIAdem. To access an individual element in scripts, use the Item property, or enter the index or the name in parentheses. |
| [`ToBarManagerUIInt.Bars`](tobarmanageruiint-bars.md) | Returns a collection of all the bars that you can use in DIAdem. To access an individual bar in a script, use the Item property or enter the index or the name in parentheses. |
| [`ToBarManagerUIInt.DataPortal`](tobarmanageruiint-dataportal.md) | Returns the Data Portal. |
| [`ToBarManagerUIInt.DesktopModified`](tobarmanageruiint-desktopmodified.md) | Specifies whether the bar definition has changed since DIAdem launched or since a desktop file was loaded or saved. DIAdem indicates that the bar definition is changed if you changed the DIAdem interface with a script or if you changed the default settings via the setting Predefine Setting in the context menu. If the DesktopModified property contains the property TRUE , a prompt asks when DIAdem closes whether DIAdem should save the changed bar definition. Assign the value FALSE to the DesktopModified property so that DIAdem does not display a confirmation prompt after the bar definition is changed. If you make changes to the bar definition after you have assigned BarManager.DesktopModified = FALSE , the DesktopModified property contains the value TRUE again. |
| [`ToBarManagerUIInt.FileName`](tobarmanageruiint-filename.md) | Returns the name of the current bar definition. The filename extension is irrelevant. Note If you do not have your own bar definition, this property contains the name Desktop . |
| [`ToBarManagerUIInt.FilePath`](tobarmanageruiint-filepath.md) | Returns the absolute path of the current bar definition. Note If you do not have your own bar definition, this property contains the name of the desktop folder. |
| [`ToBarManagerUIInt.Panels`](tobarmanageruiint-panels.md) | Returns a collection of all DIAdem panels that can contain bars. To access an individual panel in scripts, use the Item property, or enter the index or the name in parentheses. |
| [`ToBarManagerUIInt.ResourcePath`](tobarmanageruiint-resourcepath.md) | Returns the path to the folder that contains the user-defined ICO files. By default DIAdem saves the ICO files below the Desktop Folder in a subfolder with the name of the desktop file : C:\Users\ <User Name> \AppData\Local\National Instruments\DIAdem \<Version> \ <DesktopFilename> \ |
| [`ToBarManagerUIInt.Shell`](tobarmanageruiint-shell.md) | Returns the DIAdem main window. |
| [`ToBaseActionObjBaseInt.ID`](tobaseactionobjbaseint-id.md) | Specifies the name of the bar element in the ActionObj collection. DIAdem does not distinguish between uppercase and lowercase for the specified name. The name is unique in the ActionObj collection. |
| [`ToBaseActionObjBaseInt.Type`](tobaseactionobjbaseint-type.md) | Returns the bar element type. |
| [`ToDataPortalInt.UsedBars`](todataportalint-usedbars.md) | Returns a collection of all references to bars that the Data Portal contains. |
| [`ToModuleInt.ID`](tomoduleint-id.md) | Returns the panel name, which is unique in the Panel collection. |
| [`ToModuleInt.UsedBars`](tomoduleint-usedbars.md) | Returns a collection of all references to bars that a DIAdem panel contains. |
| [`ToModuleListInt.Count`](tomodulelistint-count.md) | Returns the number of DIAdem panels. |
| [`ToPropEventInt.Code`](topropeventint-code.md) | Specifies the VBS code of an event. |
| [`ToPropPictureInt.FileName`](toproppictureint-filename.md) | Returns the name of the current bar definition. The filename extension is irrelevant. |
| [`ToShellInt.UsedBars`](toshellint-usedbars.md) | Returns a collection of all references to bars that the DIAdem main window contains. |
| [`ToUsedActionObjListInt.Count`](tousedactionobjlistint-count.md) | Returns the number of references to ActionObj objects in a bar. Note If a bar contains several consecutive separating lines, DIAdem displays only one separating line. If this is the case, the number of visible bar elements does not correspond with the number of bar elements contained in the Count property. |
| [`ToUsedBarListInt.Count`](tousedbarlistint-count.md) | Returns the number of references to bars in a DIAdem panel or in the DIAdem main window. |
