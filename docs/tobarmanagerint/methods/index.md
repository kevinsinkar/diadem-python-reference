---
title: "Methods (Toolbar Manager Interface)"
---

# Methods

66 methods from `ToBarManagerInt.chm`.

| Name | Summary |
| --- | --- |
| [`ToActionObjButtonInt.GetBarID`](toactionobjbuttonint-getbarid.md) | Returns the name of the bar containing the bar element. You only can use this method in an event, such as OnClickCode . Use this method to define behavior for this bar element in relation to the position of the bar element. |
| [`ToActionObjButtonInt.Reset`](toactionobjbuttonint-reset.md) | Restores the base properties of the base type for buttons. The base properties of the buttons include the graphic to be displayed and the tooltip. |
| [`ToActionObjButtonInt.ResetCaption`](toactionobjbuttonint-resetcaption.md) | Restores the label of the base type for buttons. |
| [`ToActionObjButtonInt.ResetPicture`](toactionobjbuttonint-resetpicture.md) | Restores the graphic of the base type for buttons. |
| [`ToActionObjButtonInt.ResetTooltip`](toactionobjbuttonint-resettooltip.md) | Restores the tooltip of the base type for buttons. |
| [`ToActionObjCheckInt.GetBarID`](toactionobjcheckint-getbarid.md) | Returns the name of the bar that contains the bar element. You only can use this method in an event, such as OnClickCode . Use this method to define behavior for this bar element in relation to the position of the bar element. |
| [`ToActionObjCheckInt.Reset`](toactionobjcheckint-reset.md) | Restores the base properties of the base type for status display buttons. The base properties of the buttons include the graphic to be displayed and the tooltip. |
| [`ToActionObjCheckInt.ResetCaption`](toactionobjcheckint-resetcaption.md) | Restores the graphic of the base type for status display buttons. |
| [`ToActionObjCheckInt.ResetPicture`](toactionobjcheckint-resetpicture.md) | Restores the graphic of the base type for status display buttons. |
| [`ToActionObjCheckInt.ResetTooltip`](toactionobjcheckint-resettooltip.md) | Restores the tooltip of the base type for status display buttons. |
| [`ToActionObjListInt.Add`](toactionobjlistint-add.md) | Adds an object to the ActionObjs collection and returns an ActionObj object. |
| [`ToActionObjListInt.Copy`](toactionobjlistint-copy.md) | Duplicates an ActionObj object and adds the new object to the collection behind the object to be duplicated. |
| [`ToActionObjListInt.Exists`](toactionobjlistint-exists.md) | Checks whether a bar element with a specific name exists. |
| [`ToActionObjListInt.GetUniqueID`](toactionobjlistint-getuniqueid.md) | Checks whether the name of a bar element is valid and unique. The name of a bar element can contain the characters a to z, A to Z, 0 to 9, the hyphen, and the underscore. DIAdem replaces invalid characters with the underscore. If the specified name already exists, DIAdem generates a new name. To do so, DIAdem adds an underscore and a number to the existing name. |
| [`ToActionObjListInt.Item`](toactionobjlistint-item.md) | Returns the ActionObj object associated with a specific index or with a specific name. |
| [`ToActionObjListInt.Remove`](toactionobjlistint-remove.md) | Deletes an element from the ActionObjs collection and also deletes all references to this element in the UsedActionObjs collections. |
| [`ToActionObjListInt.RemoveAll`](toactionobjlistint-removeall.md) | Deletes all elements from the ActionObjs collection. |
| [`ToActionObjPopupInt.GetBarID`](toactionobjpopupint-getbarid.md) | Returns the name of the bar that contains the bar element. You only can use this method in an event, such as OnClickCode . Use this method to define behavior for this bar element in relation to the position of the bar element. |
| [`ToActionObjPopupInt.Reset`](toactionobjpopupint-reset.md) | Restores the base properties of the base type for buttons that open a bar. The base properties of the buttons comprise the graphic to be displayed, the tooltip, and the graphic to be opened. |
| [`ToActionObjPopupInt.ResetBarID`](toactionobjpopupint-resetbarid.md) | Replaces the name of the bar that opens when the bar element is clicked with the bar name that is entered in the base type of the bar element. |
| [`ToActionObjPopupInt.ResetCaption`](toactionobjpopupint-resetcaption.md) | Restores the graphic of the base type for buttons that open a bar. |
| [`ToActionObjPopupInt.ResetPicture`](toactionobjpopupint-resetpicture.md) | Restores the graphic of the base type for buttons that open a bar. |
| [`ToActionObjPopupInt.ResetTooltip`](toactionobjpopupint-resettooltip.md) | Restores the tooltip of the base type for buttons that open a bar. |
| [`ToBarInt.Refresh`](tobarint-refresh.md) | Rebuilds the bar and the bar elements that the bar contains and refreshes the display. To do so DIAdem executes the event OnRefreshCode for all bar elements. |
| [`ToBarListInt.Add`](tobarlistint-add.md) | Adds a bar to a Bars collection and returns an empty Bar object. |
| [`ToBarListInt.Exists`](tobarlistint-exists.md) | Checks whether a bar with a specific name exists. |
| [`ToBarListInt.GetUniqueID`](tobarlistint-getuniqueid.md) | Checks whether the name of a bar is valid and unique. The name of a bar can contain the characters a to z, A to Z, 0 to 9, the hyphen, and the underscore. DIAdem replaces invalid characters with the underscore. If the specified name already exists, DIAdem generates a new name. To do so, DIAdem adds an underscore and a number to the existing name. |
| [`ToBarListInt.Item`](tobarlistint-item.md) | Returns the Bar object associated with a specific index or with a specific name. |
| [`ToBarListInt.Remove`](tobarlistint-remove.md) | Deletes one element from the Bars collection. |
| [`ToBarListInt.RemoveAll`](tobarlistint-removeall.md) | Deletes all elements from the Bars collection. |
| [`ToBarListInt.RemoveUsedActionObj`](tobarlistint-removeusedactionobj.md) | Deletes in all bars the references to a certain bar element. |
| [`ToBarManagerInt.AddMenuBar`](tobarmanagerint-addmenubar.md) | Adds a menu bar to a bar. The method creates an ActionObjPopup object in the target bar containing the new menu bar. The method returns the Bar object which contains the new menu item. Use the AddMenuItem method to fill the new menu bar with menu items. The new bars belong to an additional bar manager not used for the display of bars in DIAdem. The methods AddMenuBar and AddMenuItem are used to create individual menu items. The new bar is usually a menu bar but can also appear as a symbol bar. DIAdem automatically allocates the IDs for the created objects. If you want to use the bars several times, use the other objects and methods of the BarManagerUnattached object. |
| [`ToBarManagerInt.AddMenuItem`](tobarmanagerint-addmenuitem.md) | Adds a menu item to a bar. The method returns the ActionObjButton object, which contains the new menu item. The new bars belong to an additional bar manager not used for the bar display in DIAdem. The methods AddMenuBar and AddMenuItem are used to create individual menu items. The new bar is usually a menu bar but can also appear as a symbol bar. DIAdem automatically allocates the IDs for the created objects. If you want to use the bars several times, use the other objects and methods of the BarManager object. |
| [`ToBarManagerInt.Create`](tobarmanagerint-create.md) | Creates a further bar manager that reads in a bar definition or that generates an empty bar definition. DIAdem does not use this bar manager for display on the interface. If you do not specify a bar definition name, DIAdem generates an empty bar definition that only contains the panels collection and two empty bars per panel. Use the empty bar definition to generate a bar definition that only contains user-defined bars which you can provide to other users. |
| [`ToBarManagerInt.CreateFromDeployment`](tobarmanagerint-createfromdeployment.md) | Unpacks a Deployment file and generates a BarManagerUnattached object from the bar definition that is created. DIAdem saves the bar definition file that the Deployment contains in the target path with the respective subfolders and the ICO files that the subfolders contain. If you do not specify a target path, DIAdem saves the files in the user folder. The returned BarManager contains the unpacked and loaded bar definition. |
| [`ToBarManagerInt.Import`](tobarmanagerint-import.md) | Imports a bar definition to add bars and bar elements to the current bar definition. For the import of a bar definition the following rules apply: DIAdem only adds bars and bar elements. DIAdem does not delete bars or bar elements. DIAdem always adds the bar elements of a source bar to an identically-named target bar. DIAdem adds the bar element to the end of the bar. If the bar to be imported does not exist, DIAdem creates it. After importing the bar, DIAdem lists the imported bars and bar elements and any errors that might occur, in the logfile. |
| [`ToBarManagerInt.IsModified`](tobarmanagerint-ismodified.md) | Specifies whether the additional bar manager, which is not used for the bar display in DIAdem, has changed compared to the saved bar definition file. |
| [`ToBarManagerInt.Load`](tobarmanagerint-load.md) | Loads a bar definition. |
| [`ToBarManagerInt.Reset`](tobarmanagerint-reset.md) | Loads the original DIAdem bar definition and refreshes the bars. |
| [`ToBarManagerInt.Save`](tobarmanagerint-save.md) | Saves the entire bar definition of an additional bar manager, which is not used for the bar display in DIAdem, under a specified name. If the file is read only, DIAdem aborts the storage process with an error message. DIAdem overwrites existing files without a requesting confirmation. |
| [`ToBarManagerInt.SaveAsDeployment`](tobarmanagerint-saveasdeployment.md) | Saves the bar definition and the user-defined ICO files in a file with the filename extension *.bdy. If the file already exists, DIAdem overwrites the file without requesting confirmation. |
| [`ToBarManagerUIInt.AddMenuBar`](tobarmanageruiint-addmenubar.md) | Adds a menu bar to a bar. The method creates an ActionObjPopup object in the target bar containing the new menu bar. The method returns the Bar object which contains the new menu item. Use the AddMenuItem method to fill the new menu bar with menu items. The methods AddMenuBar and AddMenuItem are used to create individual menu items. The new bar is usually a menu bar but can also appear as a symbol bar. DIAdem automatically allocates the IDs for the created objects. If you want to use the bars several times, use the other objects and methods of the BarManager object. |
| [`ToBarManagerUIInt.AddMenuItem`](tobarmanageruiint-addmenuitem.md) | Adds a menu item to a bar. The method returns the ActionObjButton object which contains the new menu item. The methods AddMenuBar and AddMenuItem are used to create individual menu items. The new bar is usually a menu bar but can also appear as a symbol bar. DIAdem automatically allocates the IDs for the created objects. If you want to use the bars several times, use the other objects and methods of the BarManager object. |
| [`ToBarManagerUIInt.Create`](tobarmanageruiint-create.md) | Creates a further bar manager that reads in a bar definition or that generates an empty bar definition. DIAdem does not use this bar manager for display on the interface. If you do not specify a bar definition name, DIAdem generates an empty bar definition that only contains the panels collection and three empty bars per panel. Use the empty bar definition to generate a bar definition that only contains user-defined bars which you can provide to other users. |
| [`ToBarManagerUIInt.CreateFromDeployment`](tobarmanageruiint-createfromdeployment.md) | Unpacks a Deployment file and generates a BarManager object from the bar definition that is created. DIAdem saves the bar definition file that the Deployment contains in the target path with the respective subfolders and the ICO files that the subfolders contain. If you do not specify a target path, DIAdem saves the files in the user folder. The returned BarManager contains the unpacked and loaded bar definition. |
| [`ToBarManagerUIInt.Import`](tobarmanageruiint-import.md) | Imports a bar definition to add bars and bar elements to the current bar definition. For the import of a bar definition the following rules apply: DIAdem only adds bars and bar elements. DIAdem does not delete bars or bar elements. DIAdem always adds the bar elements of a source bar to an identically-named target bar. DIAdem adds the bar element to the end of the bar. If the bar to be imported does not exist, DIAdem creates it. After importing the bar, DIAdem lists the imported bars and bar elements and any errors that might occur, in the logfile. |
| [`ToBarManagerUIInt.IsModified`](tobarmanageruiint-ismodified.md) | Specifies whether the bar definition has changed compared to the saved bar definition file. |
| [`ToBarManagerUIInt.Load`](tobarmanageruiint-load.md) | Loads a bar definition. |
| [`ToBarManagerUIInt.Refresh`](tobarmanageruiint-refresh.md) | Rebuilds the bars of the DIAdem main window and all visible, used bars of the current panel and refreshes the display. DIAdem automatically refreshes the display when scripts start or end, when the user switches panels, and when the user clicks a bar element. Therefore you do not need to call the Refresh method in a script. Use the Refresh method in the VBS code of modal and non-modal dialog boxes. |
| [`ToBarManagerUIInt.Reset`](tobarmanageruiint-reset.md) | Loads the original DIAdem bar definition and refreshes the bars. |
| [`ToBarManagerUIInt.Save`](tobarmanageruiint-save.md) | Overwrites the current bar definition if you do not specify a name for the bar definition, or saves the entire bar definition under a specified name. If you do not have your own bar definition and if you call the command without a file specification, DIAdem automatically generates a bar definition with the name of your desktop file, for example, Desktop.bdn , in the desktop folder. If the file is read only, DIAdem aborts the storage process with an error message. DIAdem overwrites existing files without a requesting confirmation. |
| [`ToBarManagerUIInt.SaveAsDeployment`](tobarmanageruiint-saveasdeployment.md) | Saves the bar definition and the user-defined ICO files in a file with the filename extension *.bdy. If the file already exists, DIAdem overwrites the file without requesting confirmation. |
| [`ToModuleInt.Refresh`](tomoduleint-refresh.md) | Rebuilds all the bars of a panel and refreshes the display. |
| [`ToModuleListInt.Exists`](tomodulelistint-exists.md) | Checks whether a panel with a specific name exists. |
| [`ToModuleListInt.Item`](tomodulelistint-item.md) | Returns the Panel object associated with a specific index or with a specific name. |
| [`ToModuleListInt.Refresh`](tomodulelistint-refresh.md) | Rebuilds all bars of all panels and refreshes the display. |
| [`ToPropEventInt.AddCodeLine`](topropeventint-addcodeline.md) | Adds a line with VBS code to the Event object. |
| [`ToUsedActionObjListInt.Add`](tousedactionobjlistint-add.md) | Adds a reference to an ActionObj object from the UsedActionObjs collection. |
| [`ToUsedActionObjListInt.Exists`](tousedactionobjlistint-exists.md) | Checks whether a bar contains a reference to a bar element. |
| [`ToUsedActionObjListInt.Find`](tousedactionobjlistint-find.md) | Searches the UsedActionObjs collection for a reference to a bar element. If you specify the start position of the search, you can also find multiple references. |
| [`ToUsedActionObjListInt.Insert`](tousedactionobjlistint-insert.md) | Adds a reference to a bar element to a specified position in the UsedActionObjs collection. If you specify a position that is larger than the number of elements that already exist, DIAdem adds the reference to the bar element at the end. Note If a bar contains several consecutive separating lines, DIAdem displays only one separating line. If this is the case, the number of visible bar elements does not correspond with the number of bar elements contained in the Count property. |
| [`ToUsedActionObjListInt.Item`](tousedactionobjlistint-item.md) | Returns the Bar element associated with a specific index or with a specific name. Note If a bar contains several consecutive separating lines, DIAdem displays only one separating line. If this is the case, the number of visible bar elements does not correspond with the number of bar elements contained in the Count property. |
| [`ToUsedActionObjListInt.Move`](tousedactionobjlistint-move.md) | Changes the position of an element in a bar. If you change the position, you also change the index of the element. |
| [`ToUsedActionObjListInt.Remove`](tousedactionobjlistint-remove.md) | Removes a reference to the UsedActionObjs collection. Note If a bar contains several consecutive separating lines, DIAdem displays only one separating line. If this is the case, the number of visible bar elements does not correspond with the number of bar elements contained in the Count property. |
| [`ToUsedActionObjListInt.RemoveAll`](tousedactionobjlistint-removeall.md) | Deletes all references to the UsedActionObjs collection. |
| [`ToUsedBarListInt.Item`](tousedbarlistint-item.md) | Returns the UsedBars object associated with a specific index or with a specific name. |
