---
title: "Methods (ICalculationSet)"
---

# Methods

53 methods from `ICalculationSet.chm`.

| Name | Summary |
| --- | --- |
| [`ICalculation.GetPostCalculations`](icalculation-getpostcalculations.md) | Returns a collection with the calculations that DIAdem does not execute until DIAdem has executed the actual calculation. The dependencies and the execution sequence of the calculations result from the dependent inputs. |
| [`ICalculation.GetPreCalculations`](icalculation-getprecalculations.md) | Returns a collection with the calculations that DIAdem executes in the specified order before executing the actual calculation. If DIAdem cannot clearly determine the calculation order, DIAdem displays an error message. The dependencies and the execution sequence of the calculations result from the dependent inputs. |
| [`ICalculation.GetValidateMessage`](icalculation-getvalidatemessage.md) | Returns information about the last execution of the Validate method which indicates why the test failed. |
| [`ICalculation.IsCrypted`](icalculation-iscrypted.md) | Specifies whether the calculation is encrypted. If the calculation is encrypted, you cannot view or edit formulas and scripts. However, you can execute the calculation. |
| [`ICalculation.Run`](icalculation-run.md) | Executes a calculation. Use Validate to execute the validation script. If a calculation requires an output from an other calculation as an input, DIAdem first executes the other calculation. DIAdem uses this method to specify the sequence in which calculations are to be executed, and automatically expands the volume of calculations to be executed, if required. If DIAdem cannot clearly determine the calculation order, DIAdem displays an error message. DIAdem records the sequence of the executed calculations in the return value so that you can reproduce the calculation sequence. If an error occurs in a calculation during the calculation, the error message contains information about the incorrect calculation. The previously executed calculations remain. |
| [`ICalculation.RunParametrized`](icalculation-runparametrized.md) | Executes the calculation without resolving the references of the inputs and outputs. Instead, the method uses the contents of the vectors that are specified as parameters. The vectors are assigned to the inputs or to the outputs via the name specified in Symbols when the index is the same. The Values vector can contain value-unit objects ( Value data type), channel objects ( Channel data type), and channel object collections ( Channel list data type). Ensure that the object type specified in Values matches the data type of the input or output that the data type Symbols references. Use the ValidateParametrized method to execute the configured validation script. |
| [`ICalculation.Validate`](icalculation-validate.md) | Checks whether DIAdem can execute the calculation. DIAdem checks whether the input references and the output references of the calculation can be resolved. DIAdem also executes the Validation script , in which you can specify further conditions for the execution of the calculation. If the calculation expects input from the output of another calculation, DIAdem also checks this calculation. DIAdem checks in the required order of the execution. If DIAdem cannot determine the execution sequence, the calculation cannot be executed. If the calculation is incorrect, DIAdem neither outputs an error message nor aborts the executing script. If the test fails, the GetValidateMessage method contains the information about the error cause. Use the ValidateSelected method of the calculation set to execute the validation scripts of all selected calculations. |
| [`ICalculation.ValidateParametrized`](icalculation-validateparametrized.md) | Checks the calculation without resolving the input and output references and executes the Validation script . Instead of the input and output references, the method uses the vector contents that are specified as parameters. The vectors are assigned to the inputs or to the outputs via the name specified in Symbols when the index is the same. The Values vector can contain value-unit objects ( Value data type), channel objects ( Channel data type), and channel object collections ( Channel list data type). Ensure that the object type specified in Values matches the data type of the input or output that the data type Symbols references. Use the RunParametrized method to execute the configured calculation script. |
| [`ICalculationCollection.Add`](icalculationcollection-add.md) | Adds a new calculation to the Calculations collection and returns a Calculation object. |
| [`ICalculationCollection.Exists`](icalculationcollection-exists.md) | Checks whether a calculation with a specific name exists. |
| [`ICalculationCollection.GetIndex`](icalculationcollection-getindex.md) | Returns the index of a calculation from a calculation group. |
| [`ICalculationCollection.Item`](icalculationcollection-item.md) | Returns the Calculations object associated with a specific name or with a specific index. |
| [`ICalculationCollection.Remove`](icalculationcollection-remove.md) | Deletes a calculation from the Calculations collection. In the existing calculations, DIAdem automatically removes the dependencies of the inputs on the deleted outputs. |
| [`ICalculationCollection.RemoveAll`](icalculationcollection-removeall.md) | Deletes all calculations from the Calculations collection. In the existing calculation groups, DIAdem automatically removes the dependencies on the deleted outputs. |
| [`ICalculationGroup.SelectAll`](icalculationgroup-selectall.md) | Selects or deselects all the calculations of the calculation group. |
| [`ICalculationGroupCollection.Add`](icalculationgroupcollection-add.md) | Adds a new calculation group to the CalculationGroups collection and returns a CalculationGroup object. |
| [`ICalculationGroupCollection.Exists`](icalculationgroupcollection-exists.md) | Checks whether a calculation group with a specific name exists. |
| [`ICalculationGroupCollection.GetIndex`](icalculationgroupcollection-getindex.md) | Returns the index of a calculation group from the current calculation set. |
| [`ICalculationGroupCollection.Item`](icalculationgroupcollection-item.md) | Returns the CalculationGroup object associated with a specific name or with a specific index. |
| [`ICalculationGroupCollection.Remove`](icalculationgroupcollection-remove.md) | Deletes a calculation group from the CalculationGroups collection. In the remaining calculations, DIAdem automatically removes the dependencies of the inputs on the deleted outputs. |
| [`ICalculationGroupCollection.RemoveAll`](icalculationgroupcollection-removeall.md) | Deletes all calculation groups from the CalculationGroups collection. |
| [`ICalculationList.Item`](icalculationlist-item.md) | Returns the Calculation object for a specific index. |
| [`ICalculationSet.Append`](icalculationset-append.md) | Adds a calculation set file ( *.tca ) to the current calculation set. When name conflicts arise, DIAdem changes the names of the added calculation and calculation groups. |
| [`ICalculationSet.Clear`](icalculationset-clear.md) | Deletes the entire contents of the calculation set. |
| [`ICalculationSet.Copy`](icalculationset-copy.md) | Copies a calculation or a calculation group within a calculation set. DIAdem assigns a new unique name to the copy. Note When copying, DIAdem also uses the inputs and the outputs of the calculation. DIAdem copies an input including the dependencies of the input. If an output has dependent inputs, DIAdem creates a copy of the output without dependencies. |
| [`ICalculationSet.Export`](icalculationset-export.md) | Exports all or selected calculations into a calculation set file ( *.tca ). |
| [`ICalculationSet.IsModified`](icalculationset-ismodified.md) | Specifies whether the calculation set has changed compared to the saved calculation set file. |
| [`ICalculationSet.Load`](icalculationset-load.md) | Loads a calculation set from a file that has the filename extension .tca . In doing so, DIAdem overwrites any existing calculation set. If the calculation set file to be loaded is encrypted, you can execute the calculations that the file contains. However you cannot view or edit the calculations. |
| [`ICalculationSet.Move`](icalculationset-move.md) | Changes the position of a calculation in a calculation group or the position of a calculation group in a calculation set. If the change causes a name conflict, DIAdem automatically changes the name of the calculation or of the calculation group. |
| [`ICalculationSet.RunSelected`](icalculationset-runselected.md) | Executes selected calculations. The Selected property of a calculation specifies whether the calculation is selected. If a calculation requires an output from an other calculation as an input, DIAdem first executes the other calculation. DIAdem uses this method to specify the sequence in which calculations are to be executed, and automatically expands the volume of calculations to be executed, if required. If DIAdem cannot clearly determine the calculation order, DIAdem displays an error message. DIAdem records the sequence of the executed calculations in the return value so that you can reproduce the calculation sequence. If an error occurs in a calculation during the calculation, the error message contains information about the incorrect calculation. The previously executed calculations remain. Use the Validate method of a calculation to execute the validation script of the respective calculation. |
| [`ICalculationSet.Save`](icalculationset-save.md) | Saves the current calculation set in the current calculation set file. The FileName property contains the name of the calculation set file. |
| [`ICalculationSet.SaveAs`](icalculationset-saveas.md) | Saves the current calculation set in a file with the filename extension .tca . |
| [`ICalculationSet.SelectAll`](icalculationset-selectall.md) | Selects or deselects all calculations. |
| [`ICalculationSet.ShowExecutionDlg`](icalculationset-showexecutiondlg.md) | Opens the dialog box for the execution of calculations. |
| [`ICalculationSet.ShowSettingsDlg`](icalculationset-showsettingsdlg.md) | Opens the dialog box for editing a calculation set. |
| [`ICalculationSet.ValidateSelected`](icalculationset-validateselected.md) | Checks whether DIAdem can execute the selected calculations. The Selected property of a calculation specifies whether the calculation is selected. DIAdem checks whether the input references and the output references of each calculation can be removed. DIAdem also executes the Validation script in which you can specify further conditions for the execution of each calculation. If a calculation expects input from the output of another calculation, DIAdem also checks this calculation. DIAdem checks in the required order of the execution. If DIAdem cannot determine the execution sequence, the calculation cannot run. If the inspection fails, DIAdem writes the list of incorrect calculations in the FailedCalculations parameter. Use the Validate method of a calculation to execute the validation script of each calculation. |
| [`IInput.ConnectOutput`](iinput-connectoutput.md) | Changes an existing input in such a way that the input is dependent on the output of a calculation. DIAdem transfers the properties of the specified output to the dependent input. However, the input name remains the same. You cannot edit the transferred properties at the input. If the output properties change, these changes influence all dependent inputs. DIAdem also extends the output property TargetInputs . |
| [`IInput.DisconnectOutput`](iinput-disconnectoutput.md) | Removes the dependency of an input on an output of a different calculation. The properties transferred by the output to the dependent input remain the same. After the dependency is removed, you can edit these properties. |
| [`IInput.IsOutputConnected`](iinput-isoutputconnected.md) | Specifies whether the input of a calculation is dependent on an output of a different calculation. |
| [`IInputCollection.Add`](iinputcollection-add.md) | Adds a new input to the Inputs collection and returns an Input object. |
| [`IInputCollection.AddByOutput`](iinputcollection-addbyoutput.md) | Generates an input that is dependent on an output of a calculation and adds this input to the Inputs collection. The properties of the generated input correspond to the properties of the specified output. You can edit the name of the input afterwards. You cannot change the other properties from the specified output. If the output properties change, the changes influence all dependent inputs. If the name of the specified output already exists in the Inputs or Outputs collection, DIAdem does not generate a new object, it displays an error message. If the dependent input is correct, DIAdem also extends the TargetInputs output property. |
| [`IInputCollection.Exists`](iinputcollection-exists.md) | Checks whether a calculation contains an input with a specific name. |
| [`IInputCollection.Item`](iinputcollection-item.md) | Returns the Input object associated with a specific name or with a specific index. |
| [`IInputCollection.Move`](iinputcollection-move.md) | Changes the position of an input in the list of inputs of the calculation. |
| [`IInputCollection.Remove`](iinputcollection-remove.md) | Deletes an input from the Inputs collection of a calculation. |
| [`IInputCollection.RemoveAll`](iinputcollection-removeall.md) | Deletes all inputs from the Inputs collection of a calculation. |
| [`IInputList.Item`](iinputlist-item.md) | Returns the Input object of a specific index. |
| [`IOutputCollection.Add`](ioutputcollection-add.md) | Adds a new output to the Outputs collection and returns an Output object. |
| [`IOutputCollection.Exists`](ioutputcollection-exists.md) | Checks whether a calculation contains an output with a specific name. |
| [`IOutputCollection.Item`](ioutputcollection-item.md) | Returns the Output object for a specific name or for a specific index. |
| [`IOutputCollection.Move`](ioutputcollection-move.md) | Changes the position of an output in the list of outputs of the calculation. |
| [`IOutputCollection.Remove`](ioutputcollection-remove.md) | Deletes an output from the Outputs collection of a calculation. In the existing calculations, DIAdem automatically removes the dependencies of the inputs on the deleted output. |
| [`IOutputCollection.RemoveAll`](ioutputcollection-removeall.md) | Deletes all outputs from the Outputs collection of a calculation. In the existing calculation groups, DIAdem automatically removes the dependencies of the inputs on the deleted outputs. |
